import 'package:flutter/material.dart';




class ajouteformation extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
    body: Container(),
    appBar: AppBar(
      title: Text('Ajouter Formation'),
      centerTitle: true,
      backgroundColor: Colors.blue,
    ),
  );
}
